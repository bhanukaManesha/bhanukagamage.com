language: node_js
node_js:
 - "8.11"

branches:
  only:
    - master

addons:
  chrome: stable

before_script:
  - npm install -g @angular/cli 
  - npm install n -g
  - n stable

cache:
  yarn: true
  directories:
    - node_modules

script:
  - ng build --prod

notifications:
  email : "bhanukamgamage@gmail.com"

# deploy:
#   provider: pages
#   skip-cleanup: true
#   github-token: $GITHUB_TOKEN
#   keep-history: true
#   local-dir: ./dist/
#   on: